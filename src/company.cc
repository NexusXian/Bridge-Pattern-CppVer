#include "../includes/company.h"
int Company::calculateSalary(std::string const &codes) { return 0; }

Company::Company(const std::string &name, const std::string &place)
    : name_(name), place_(place) {}
